/**
 * Narrow lucide surface for the always-mounted shell chrome.
 * Feature modules keep their own local lucide imports.
 */
export { Network, FileJson, Database, Settings, Plus } from 'lucide-react'
